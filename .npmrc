# The community-nodes lint plugin pins eslint@9 as a peer while the legacy
# lint still runs eslint@8 — both lint passes work (see eslint.config.mjs),
# npm just must not hard-fail on that peer mismatch during install/ci.
legacy-peer-deps=true
